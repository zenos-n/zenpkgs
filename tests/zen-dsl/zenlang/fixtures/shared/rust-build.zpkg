_meta = {
  name = "rustc";
  summary = "Rust compiler";
  description = ''Pinned Nixpkgs Rust compiler'';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ "development" ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.rustc;

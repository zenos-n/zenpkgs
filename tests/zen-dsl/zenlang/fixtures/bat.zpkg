_meta = {
  name = "bat";
  summary = "A cat(1) clone with wings";
  description = ''Syntax-highlighting file viewer'';
  zenosVersion = "1.0.0";
  packageVersion = "0.24.0";
  tags = [ "cli" "viewer" ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ $pkgs.legacy.pkg-config ];
    runtime = [ ];
  };
};

import $pkgs.legacy.bat;

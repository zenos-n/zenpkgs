_meta = {
  zenosVersion = 1.0.0Na;
  summary = "ZenOS system package selectors";
  description = ''
    System package selectors are boolean leaves resolved by the structure runtime
    against filesystem-derived paths in pkgs.zenos.
  '';
  maintainers = [ $m.doromiert ];
  license = $l.napl;
  platforms = $lib.platforms.zenos;
};

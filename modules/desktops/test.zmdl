    meta = {
        brief = "action";
        description = "bin";
        maintainers = [ maintainers.doromiert ];
        license = "napalm";
        dependencies = [ "systemd" ];
    };

    options = {
        enable = enableOption {
            meta.brief = "Enable the feature service.";
            action = { cfg, isUser }: {
                legacy.services.something.enable = true;
            };
        };

        mode = {
            type = "string";
            default = "strict";
            meta = {
                brief = "Option description for docs.nix.";
            };
        };
    };

    action = localConfig: isUser: {
        # Global node actions (evaluated continuously)
    };

    legacy = {
        # Direct standard NixOS passthrough (if needed)
    };
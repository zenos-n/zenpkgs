_meta = {
  name = "podman-desktop";
  summary = "Curated podman-desktop package for ZenOS";
  description = ''
    Curated podman-desktop package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.podman-desktop;

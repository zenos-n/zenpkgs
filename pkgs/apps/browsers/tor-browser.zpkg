_meta = {
  name = "tor-browser";
  summary = "Curated tor-browser package for ZenOS";
  description = ''
    Curated tor-browser package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.tor-browser;

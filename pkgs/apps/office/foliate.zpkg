_meta = {
  name = "foliate";
  summary = "Curated foliate package for ZenOS";
  description = ''
    Curated foliate package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.foliate;

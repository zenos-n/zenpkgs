_meta = {
  name = "apostrophe";
  summary = "Curated apostrophe package for ZenOS";
  description = ''
    Curated apostrophe package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.apostrophe;

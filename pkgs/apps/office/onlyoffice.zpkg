_meta = {
  name = "onlyoffice";
  summary = "Curated onlyoffice package for ZenOS";
  description = ''
    Curated onlyoffice package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.onlyoffice-desktopeditors;

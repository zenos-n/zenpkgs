_meta = {
  name = "vscode";
  summary = "Curated vscode package for ZenOS";
  description = ''
    Curated vscode package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.vscode;

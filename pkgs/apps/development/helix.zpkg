_meta = {
  name = "helix";
  summary = "Curated helix package for ZenOS";
  description = ''
    Curated helix package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.helix;

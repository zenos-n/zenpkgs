_meta = {
  name = "fish";
  summary = "Curated fish package for ZenOS";
  description = ''
    Curated fish package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.fish;

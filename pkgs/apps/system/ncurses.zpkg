_meta = {
  name = "ncurses";
  summary = "Curated ncurses package for ZenOS";
  description = ''
    Curated ncurses package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.ncurses;

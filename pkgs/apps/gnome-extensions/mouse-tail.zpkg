_meta = {
  name = "mouse-tail";
  summary = "Curated mouse-tail package for ZenOS";
  description = ''
    Curated mouse-tail package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ "system" ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.gnomeExtensions.mouse-tail;

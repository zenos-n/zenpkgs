_meta = {
  name = "steam";
  summary = "Curated steam package for ZenOS";
  description = ''
    Curated steam package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.steam;

_meta = {
  name = "rsync";
  summary = "Curated rsync package for ZenOS";
  description = ''
    Curated rsync package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ "system" ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.rsync;

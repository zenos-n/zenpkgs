_meta = {
  name = "squashfs-tools";
  summary = "Curated squashfs-tools package for ZenOS";
  description = ''
    Curated squashfs-tools package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ "system" ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.squashfsTools;

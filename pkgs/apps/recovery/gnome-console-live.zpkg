_meta = {
  name = "gnome-console-live";
  summary = "Curated gnome-console-live package for ZenOS";
  description = ''
    Curated gnome-console-live package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ "system" ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.gnome-console;

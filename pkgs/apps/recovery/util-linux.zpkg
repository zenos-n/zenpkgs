_meta = {
  name = "util-linux";
  summary = "Curated util-linux package for ZenOS";
  description = ''
    Curated util-linux package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ "system" ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.util-linux;

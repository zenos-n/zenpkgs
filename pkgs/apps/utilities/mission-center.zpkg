_meta = {
  name = "mission-center";
  summary = "Curated mission-center package for ZenOS";
  description = ''
    Curated mission-center package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.mission-center;

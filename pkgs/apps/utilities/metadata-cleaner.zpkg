_meta = {
  name = "metadata-cleaner";
  summary = "Curated metadata-cleaner package for ZenOS";
  description = ''
    Curated metadata-cleaner package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.metadata-cleaner;

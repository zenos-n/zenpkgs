_meta = {
  name = "gnome-boxes";
  summary = "Curated gnome-boxes package for ZenOS";
  description = ''
    Curated gnome-boxes package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.gnome-boxes;

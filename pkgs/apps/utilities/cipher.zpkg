_meta = {
  name = "cipher";
  summary = "Curated cipher package for ZenOS";
  description = ''
    Curated cipher package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.decoder;

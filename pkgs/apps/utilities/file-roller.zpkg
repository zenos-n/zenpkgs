_meta = {
  name = "file-roller";
  summary = "Curated file-roller package for ZenOS";
  description = ''
    Curated file-roller package for ZenOS
  '';
  zenosVersion = "1.0.0";
  packageVersion = "";
  tags = [ ];
  maintainers = [ $m.doromiert ];
  dependencies = {
    general = [ ];
    build = [ ];
    runtime = [ ];
  };
};

import $pkgs.legacy.file-roller;
